## Simple Example

```shell
bundle install
```

Start the server:

```shell
bundle exec ruby ./hello_s.rb
```

Start the client:

```shell
bundle exec ruby ./hello_c.rb
```

Should output on the server:

```shell 
Hello, world!
```
